# web — goszakup-it-radar dashboard

React + TypeScript + Vite + Tailwind frontend for the dashboard. See the
[project README](../README.md) for the full picture.

```bash
npm install
cp .env.example .env.local   # VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY
npm run dev
```
